services:
  identity:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATABASE_URL: file:/app/data/db.sqlite
    volumes:
      - ./data:/app/data
    restart: unless-stopped
